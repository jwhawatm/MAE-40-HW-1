clc; 
clear all; 
close all;

%Example 9.1
%Calling parameters
s = tf('s');
d = (0.1);
n =[1 2 4 8];
%bode(tf([num,den]))

F1 = (1-(d*s)/2)/(1+(d*s)/2);
[num den] = tfdata(F1, 'v')

figure(1)
pzmap(F1)
grid on


%% 
clc; 
clear all; 
close all;


s = tf('s');
d = (0.1);
n =[1 2 4 8];
F2 = (1-((d*s)/2)+((d*s)^2)/12)/(1+((d*s)/2)+((d*s)^2)/12);
[num den] = tfdata(F2, 'v')

figure(2)
pzmap(F2)
grid on


%% 
clc; 
clear all; 
close all;



s = tf('s');
d = (0.1);
n =[1 2 4 8];
F4 = (1 - ((d*s)/2) +3*((d*s)^2)/28 - ((d*s)^3)/84 +((d*s)^4)/1680)/(1 + ((d*s)/2) + 3*((d*s)^2)/28 + ((d*s)^3)/84 +((d*s)^4)/1680);
[num den] = tfdata(F4, 'v')

figure(3)
pzmap(F4)
grid on


%% 
clc; 
clear all; 
close all;

s = tf('s');
d = (0.1);
n =[1 2 4 8];
F8 = (1 - (d*s)/2 + 3*((d*s)^2)/35 - ((d*s)^3)/60 +((d*s)^4)/624 -((d*s)^5)/9360 + ((d*s)^6)/205920 -((d*s)^7)/7207200 + ((d*s)^8)/518918400)/(1 + (d*s)/2 + 3*((d*s)^2)/35 + ((d*s)^3)/60 +((d*s)^4)/624 +((d*s)^5)/9360 + ((d*s)^6)/205920 +((d*s)^7)/7207200 + ((d*s)^8)/518918400);
[num den] = tfdata(F8, 'v')

figure(4)
pzmap(F8)
grid on

